apiVersion: v1
kind: Service
metadata:
  name: {{ include "clapp.fullname" . }}
  labels:
    {{- include "clapp.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "clapp.selectorLabels" . | nindent 4 }}
