apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "clapp.fullname" . }}-test-connection"
  labels:
    {{- include "clapp.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "clapp.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
